using System;

namespace mergeSort
{
    class mSort
    {
        static void Main()
        {
            int n = int.Parse(Console.ReadLine());

            int[] arrayOriginal = new int[n];

            for (int i = 0; i < n; i++)
            {
                arrayOriginal[i] = int.Parse(Console.ReadLine());
            }

            //Array.Sort(arrayOriginal);

            //foreach (int item in arrayOriginal)
            //{
            //    Console.WriteLine(item);
            //}

            // split all elements in separate arrays

            for (int i = 0; i < n; i++)
            {

            }





        }
    }
}
