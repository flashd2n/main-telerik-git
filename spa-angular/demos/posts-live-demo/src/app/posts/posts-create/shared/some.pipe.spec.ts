/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SomePipe } from './some.pipe';

describe('Pipe: Somee', () => {
  it('create an instance', () => {
    let pipe = new SomePipe();
    expect(pipe).toBeTruthy();
  });
});